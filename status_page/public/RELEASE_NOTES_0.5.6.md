# Release notes — Restore Privacy **0.5.6**

**Catalog monopin:** 0.5.6  
**Paid installers:** Windows, Linux, Android, **macOS** (Developer ID + notarized), **iOS** (Team-signed sideload).

## Highlights

- **macOS** catalog zip sealed with **Developer ID Application** (Team **SFCBP95595**), notarized and stapled.
- **iOS** catalog zip sealed with **Apple Distribution** (Runner + Packet Tunnel extension).
- Residual **IPv4** is always on (full-tunnel capture); residual **IPv6** remains a Settings switch (default ON).
- macOS tray / dock restore reopens the window without dropping residual.
- Paid store monopin **0.5.6** on Helsinki.

## Packages

| Platform | File |
|----------|------|
| Windows | `restore-privacy-client-0.5.6-windows-x64-setup.exe` |
| Linux | `restore-privacy-client-0.5.6-linux-x64.tar.gz` |
| Android | `restore-privacy-client-0.5.6-android.apk` |
| macOS | `restore-privacy-client-0.5.6-macos.zip` |
| iOS | `restore-privacy-client-0.5.6-ios.zip` |

After payment: one-time download + email with **keygen** + **PPI**. Residual Connect requires a valid keygen while subscription is OK.
