# Deploy rpOS (operator)

See also admin-only page on the status host: `/admin/rpos` (requires admin login).

1. Commercial deposit / Service path (£3000 deposit framing).
2. Stage Suite + Rx packages on paid host.
3. Customer RESTORE flow with wipe warning (desktop); mobile honesty.
4. Configure custom network via separate company SDK admin installer.
5. Enable Ned (rpAI) narrative helper for install storytelling.
6. Record handoff in admin accounting.
