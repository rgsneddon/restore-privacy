# rpMail (email client SDK surface)

Private repo: **https://github.com/rgsneddon/rpMail**

```bash
git clone https://github.com/rgsneddon/rpMail.git
cd rpMail
python3 -m rpmail --smoke
python3 -m unittest discover -s tests -v
```

Company mailboxes: **moderator-only** (`rpmail.roles`).
