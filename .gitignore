# Secrets / keys (never commit)
secrets/
**/secrets/
*.pem
*.key
!*.pub.example
.env
.env.*
client_app/**/secrets/

# Flutter build artifacts
client_app/build/
client_app/.dart_tool/
client_app/.flutter-plugins-dependencies

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log

# Built release packages (uploaded to GitHub Releases)
releases/
dist/


# Swift package build (RPT2 engine tests)
client_app/apple_shared/Rpt2/.build/
client_app/apple_shared/Rpt2/.swiftpm/

# Paid download SQLite / local payment data
status_page/data/


# Staged paid installers (deploy via host disk or CI; use RPT_GITHUB_TOKEN on Render if not staged)
status_page/assets/*/
!status_page/assets/README.md


_tmp_pyz.pyz

.venv-brand/

# Node priv keys never committed (deploy-only local)
product/.*.priv*
*.priv.deploy


# Stripe Dashboard-issued DNS (local only)
stripe_email_domain_dns.local.json
stripe_email_dns_report.json

