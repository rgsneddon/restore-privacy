import 'package:flutter/material.dart';

/// Win 3.1 retro palette + exact scrolling privacy string.
const String kScrollingPrivacyText =
    'lightweight vpn to restore your privacy - no user data is retained - your privacy is restored';

const String kAppTitle = 'RESTORE PRIVACY';
const String kBannerTitle = 'Restore Privacy - Tunnel Client';

const Color kBannerBg = Color(0xFF000080); // dark blue
const Color kWindowBg = Color(0xFF000000); // black
const Color kWindowFg = Color(0xFFFFFFFF); // white
const Color kStatusFg = Color(0xFFC0C0C0);
