# Apple handoff 0.2.0

Production RPT node: **82.221.101.241:44044** (UDP).

macOS/iOS zips on GitHub Release 0.2.0 may be **prep packages** staged from prior builds
when this host cannot run `flutter build macos/ios`. Rebuild on a Mac with
`client_app` sources (RptEndpoint.host = 82.221.101.241) before production residual use.
